viewWidth           = 1200px

$container
    width           : viewWidth

    max-width       : 100%

    padding         : 0 15px
    margin          : 0 auto

container = @block {
    width           : viewWidth

    max-width       : 100%

    padding         : 0 15px
    margin          : 0 auto
}