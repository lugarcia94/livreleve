/**
 * VTex Local - Variaveis
 */

// ----- Config ----- //

// Color
color         = #DEB94C #333333 #F0F0F0
bg            = rgba(255,255,255, .85)

// Fonts
family        = unquote("'Source Sans Pro', sans-serif")

light         = 300
regular       = 400
medium        = 500
semi-bold     = 600
bold          = 700

// Animations
transition    = unquote("all .2s ease-in-out")

// Global